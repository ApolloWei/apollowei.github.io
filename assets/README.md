# Assets

Place real aerial media here when it is ready:

- `images/` for cover images, thumbnails, and photo sets
- `videos/` for exported aerial videos

The current site uses CSS placeholders so the framework works before real media is added.
